// 초대 링크를 한 번 쓰고 나면 주소에서 지운다.
//
// 웹은 해시 전략이라 `?program=…` 이 `#` 앞에 남는다. 그대로 두면 등록을
// 마치고 홈으로 간 뒤 **새로고침할 때마다 다시 등록 화면으로 끌려간다** —
// 페이지가 다시 열릴 때마다 main() 이 그 값을 또 읽기 때문이다.
//
// 지우는 시점은 "링크를 실제로 소비한 순간"이다. 시작하자마자 지우면
// 로그인 도중에 새로고침한 사람이 목적지를 잃는다.
export 'join_query_cleanup_io.dart'
    if (dart.library.js_interop) 'join_query_cleanup_web.dart';
